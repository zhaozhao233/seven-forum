server:
# 端口
  port: 8090
#  servlet:
#   上下文路径
#    context-path: /api
spring:
  # 导入其他配置文件
  profiles:
    include: dao

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 100MB

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8